services:
  rogue-dns:
    build: ./
    restart: unless-stopped
    environment:
      TZ: 'America/Denver'
    volumes:
      # Keeps the Cloudflare API token out of the image. Read-only: the tool never writes it.
      - ./config.ini:/opt/rogue-dns/config.ini:ro
